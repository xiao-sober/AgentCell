# AgentCell Web

此目录是 React 工作台的预留工作区。进入产品接口阶段后，使用 Vite、React、TypeScript、pnpm、TanStack Query、Vitest 和 Playwright 初始化。

在新增页面或组件前，先检查 `src/components`、`src/components/ui`、`src/components/common`、`src/features` 和 Storybook，优先复用项目已有组件。前端只消费稳定 DTO，不复制后端状态机、预算或权限逻辑。
